# jw-calendar

Fetches class schedules from USP's JupiterWeb and exports a RFC 5545 compatible calendar file.

## Usage:

Set `ID_USP` and `ID_PASS` environment variables accordingly and run `jw_calendar.py [calendar file]`.
